{
  "name": "recruiting-agent-site",
  "private": true,
  "description": "The download page. index.html is the whole site; the test is here because this page is the only funnel every user passes through.",
  "scripts": {
    "test": "node test/download-page.mjs",
    "test:live": "node test/download-page.mjs --live"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
